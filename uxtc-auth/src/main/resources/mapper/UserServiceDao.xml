<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uxtc.auth.cloud.mapper.UserServiceDao">
    <!--    查询用户详情-->
    <select id="loadUserByUsername" resultType="com.uxtc.common.cloud.entity.UserDto">
        SELECT
            id,
            username,
            password,
            status
        FROM tb_user
        WHERE username = #{userName}
    </select>

    <!--    查询用户权限-->
    <select id="getRoles" resultType="java.lang.String">
        SELECT
            a.url
        FROM
            tb_permission a
            LEFT JOIN tb_role_permission b ON a.id = b.permission_id
            LEFT JOIN tb_user_role c ON b.role_id = c.role_id WHERE c.user_id = #{id}
    </select>
</mapper>